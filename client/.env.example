# Default gateway URL (can be overridden in the UI)
VITE_DEFAULT_GATEWAY_URL=http://localhost:8080

# WalletConnect project ID — get one free at https://cloud.reown.com
VITE_WALLETCONNECT_PROJECT_ID=your_project_id_here

# Optional: pre-load a private key for development (DO NOT use in production)
# VITE_DEV_PRIVATE_KEY=0x...
